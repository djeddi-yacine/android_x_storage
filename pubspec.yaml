name: android_x_storage
description: A new Flutter plugin for Android to get the external storage directories.

homepage: https://github.com/dj-yacine-flutter/android_x_storage
repository: https://github.com/dj-yacine-flutter/android_x_storage
issue_tracker: https://github.com/dj-yacine-flutter/android_x_storage/issues
version: 0.1.0

topics:
  - android
  - storage

screenshots:
  - description: Android Phone.
    path: screenshots/screenshot_01.png
  - description: Android TV.
    path: screenshots/screenshot_02.png

environment:
  sdk: '>=3.0.3 <4.0.0'
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: dev.yacine.android_x_storage
        pluginClass: AndroidXStoragePlugin

